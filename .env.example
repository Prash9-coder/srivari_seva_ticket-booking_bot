# Backend environment variables
# Comma-separated list of allowed frontend origins for CORS
FRONTEND_ORIGINS=https://your-frontend.vercel.app,https://your-custom-domain
# Bind host and port
HOST=0.0.0.0
PORT=8000